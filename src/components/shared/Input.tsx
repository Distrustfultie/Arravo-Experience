import {InputHTMLAttributes} from "react";
export function Input({label,id,error,hint,className="",...p}:{label:string;id:string;error?:string;hint?:string}&InputHTMLAttributes<HTMLInputElement>){return <div className="space-y-2"><label htmlFor={id} className="block text-sm font-bold">{label}</label><input id={id} className={`w-full rounded-2xl border bg-white px-4 py-3.5 text-base outline-none focus:border-tomato-500 focus:ring-2 focus:ring-tomato-100 ${error?"border-tomato-500":"border-neutral-200"} ${className}`} {...p}/>{hint&&!error&&<p className="text-xs text-neutral-500">{hint}</p>}{error&&<p className="text-xs font-semibold text-tomato-700">{error}</p>}</div>}